fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
openai>=1.30.0
reportlab==4.2.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
requests==2.32.3

# --- Invoice OCR (PP-OCRv5). Apache-2.0, self-hosted, CPU-only, no per-page cost.
# Optional at runtime: /api/ocr/health reports unavailable and the Next.js
# pipeline falls back to its bundled engine if these are not installed.
paddlepaddle>=3.0.0
paddleocr>=3.0.0
pillow>=10.0.0
numpy>=1.24.0
